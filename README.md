# aashik-project

### How to run the file

1. Clone the repository using `git clone https://github.com/aashikpaul/aashik-project.git`.
2. Navigate to the project directory using `cd aashik-project`.
3. Run the file using `python3 main.py`.
4. 
